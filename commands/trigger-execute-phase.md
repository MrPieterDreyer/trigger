---
name: trigger-execute-phase
description: Execute all tasks in a phase through the build-review pipeline
---

Run the execute-phase skill. Usage: "trigger execute phase N" where N is the phase number. Sequentially builds each task through the full Builder → Reviewer → QA → Sign-off pipeline.
